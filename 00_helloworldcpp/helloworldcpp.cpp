// helloworldcpp.cpp
//
// This file is the most basic of demos.

// This demo uses a standard C++ header, so it must be compiled with a C++ compatible compiler, such as g++ or clang++.
// Most of what we will be doing in these demos will not be using standard libraries,
// so upcoming demos will use .h style headers, but it will still mostly be using C++.
#include <iostream>

using namespace std;

int main()
{
	// Ni hao, shi jie
	cout << "你好,世界 from C++!\n";
	return 0;
}
